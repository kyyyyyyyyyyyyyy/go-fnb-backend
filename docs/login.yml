info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8081/login
  body:
    type: json
    data: |-
      {
        "email": "tengsi@uniku.ac.usa",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
