info:
  name: register
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/register
  body:
    type: json
    data: |-
      {
        "name": "tengsi",
        "email": "coba1@gmail.com",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
